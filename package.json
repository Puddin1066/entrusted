{
  "name": "entrusted-pereira",
  "private": true,
  "description": "Monorepo root. On Vercel: Project Settings → Build & Deployment → Root Directory MUST be \"entrusted\" (do not deploy from repo root). Local: npm run dev --prefix entrusted.",
  "scripts": {
    "dev": "npm run dev --prefix entrusted",
    "build": "npm run build --prefix entrusted",
    "start": "npm run start --prefix entrusted",
    "lint": "npm run lint --prefix entrusted"
  }
}
