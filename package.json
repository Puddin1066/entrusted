{
  "name": "entrusted-pereira",
  "private": true,
  "description": "Monorepo root. Prefer Vercel Root Directory \"entrusted\". Root lists next so repo-root deploys are detected as Next.js (not static \"Other\"/public).",
  "dependencies": {
    "next": "^16.2.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "scripts": {
    "postinstall": "npm install --prefix entrusted",
    "dev": "npm run dev --prefix entrusted",
    "build": "npm run build --prefix entrusted",
    "start": "npm run start --prefix entrusted",
    "lint": "npm run lint --prefix entrusted"
  }
}
