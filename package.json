{
  "name": "entrusted-pereira",
  "private": true,
  "description": "Monorepo root. Vercel: Root Directory \"entrusted\"; Framework Preset \"Next.js\" (not Other). entrusted/vercel.json forces nextjs. postinstall installs app deps if build runs from repo root.",
  "scripts": {
    "postinstall": "npm install --prefix entrusted",
    "dev": "npm run dev --prefix entrusted",
    "build": "npm run build --prefix entrusted",
    "start": "npm run start --prefix entrusted",
    "lint": "npm run lint --prefix entrusted"
  }
}
